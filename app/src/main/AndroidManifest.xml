<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.roman_druck.ampg_printing_room">

    <application
        android:name=".activities.MainApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ampg_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ampg_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AMPG_Printing_Room"
        tools:targetApi="31">
        <activity
            android:name=".activities.NewOffsetNotesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>